server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback (frontend calls the backend directly via VITE_API_URL)
    location / {
        try_files $uri /index.html;
    }
}
